---
sidebar_position: 3
title: Database Schema
---

import ERDEmbed from '@site/src/components/ERDEmbed';
import { dbNodes, dbEdges } from '@site/src/components/ERDEmbed/schema-data';

# Database Schema

This page presents the database structure of ERD Builder Pro visually. The diagram below represents the main tables stored in the PostgreSQL database (**Supabase** mode) and how the data relates to each other.

## Database Structure Visualization

Use the interactive diagram below to understand the data flow between tables. You can drag tables or zoom in to view column details.

<ERDEmbed height="650px" nodes={dbNodes} edges={dbEdges} />

:::tip Navigation
Arrow lines indicate *Foreign Key* relationships between tables. Different colors help you identify table categories (e.g., Purple for Project, Pink for Diagram).
:::

---

## Full SQL (DDL) Code

For developers who want to perform a self-hosted installation or database migration, please use the full SQL schema below:

<details>
<summary>View Full SQL Schema (Supabase)</summary>

```sql
-- Supabase Schema for ERD Builder Pro

-- Projects Table
CREATE TABLE IF NOT EXISTS projects (
  id BIGSERIAL PRIMARY KEY,
  uid UUID DEFAULT gen_random_uuid() UNIQUE,
  name TEXT NOT NULL,
  user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL,
  is_deleted BOOLEAN DEFAULT FALSE,
  deleted_at TIMESTAMPTZ DEFAULT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  is_public BOOLEAN DEFAULT FALSE,
  share_token TEXT,
  expiry_date TIMESTAMPTZ
);

-- Diagrams Table (ERD Files)
CREATE TABLE IF NOT EXISTS diagrams (
  id BIGSERIAL PRIMARY KEY,
  uid UUID DEFAULT gen_random_uuid() UNIQUE,
  name TEXT NOT NULL,
  user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL,
  project_id BIGINT REFERENCES projects(id) ON DELETE SET NULL,
  is_deleted BOOLEAN DEFAULT FALSE,
  deleted_at TIMESTAMPTZ DEFAULT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  viewport_x FLOAT DEFAULT 0,
  viewport_y FLOAT DEFAULT 0,
  viewport_zoom FLOAT DEFAULT 1.0,
  is_public BOOLEAN DEFAULT FALSE,
  share_token TEXT,
  expiry_date TIMESTAMPTZ,
  published_at TIMESTAMPTZ,
  _version INTEGER DEFAULT 0
);

-- [Other table data...]
-- Full details can be found in the supabase_schema.sql file in the main repository.
```

</details>

### Main Table Summary:
*   **`projects`**: Main container for all files.
*   **`diagrams`**: Parent table for ERD files.
*   **`entities`**: Stores individual tables within a diagram.
*   **`columns`**: Stores fields/columns of each entity.
*   **`relationships`**: Stores relationship lines between tables.
*   **`entity_changes`**: Audit Trail system for version history (Snapshot).